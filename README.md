# Поиск недвижимости

- Laravel 13 + Jetstream (Inertia) + Vue 3 + Element Plus + PostgreSQL + Docker.

## Развёртывание

```bash
./setup.sh
```

**http://localhost:8088**

- После запуска ждать миграцию и заполнения БД
